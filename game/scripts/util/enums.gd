extends Resource
class_name Enums

enum GameMode { SURVIVAL, KOTH, CTF }

const AGENT_SETTING_FOV := [45, 60, 90, 120]   # degrees
const AGENT_SETTING_LOS := [500, 1200, 1700]   # S = 500, M = 1200, L = 1700
const AGENT_SETTING_SPEED := [100, 200, 300]   # SLOW = 100, MID = 200, FAST = 300

const ROUNDS_SETTING_DURATION := [30, 60, 90]  # seconds
const ROUNDS_SETTING_NUMBER := [1, 2, 3, 4, 5]
